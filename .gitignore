# Node modules
node_modules/
client/node_modules/    # Exclude frontend node_modules
server/node_modules/    # Exclude backend node_modules

# dotenv environment variables
.env                    # Exclude a root .env if it exists
client/.env             # Exclude client's .env if used (Vite often uses .env.local)
client/.env.local       # Exclude client's local development .env
server/.env             # Exclude backend's .env (CRITICAL for secrets)

# Build outputs
dist/                   # Vite's default build output for client
dist-ssr/
build/                  # Common build output name
client/build/           # Specific client build folder
server/build/           # Specific server build folder
.vite/                  # Vite cache directory

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json # Keep extensions.json to share recommended extensions
.idea/                  # IntelliJ IDEA projects
.DS_Store               # macOS specific file
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?